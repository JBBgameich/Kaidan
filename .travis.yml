language: cpp
compiler: gcc
cache: ccache
sudo: required
dist: trusty
services:
  - docker
install:
  - ./utils/travis-dependencies.sh
script:
  - cd utils
  - ./travis-build.sh
matrix:
  include:
    - env: BUILD_SYSTEM=qmake
    - env: BUILD_SYSTEM=cmake
    - env: BUILD_VARIANT=click
notifications:
  email: false
